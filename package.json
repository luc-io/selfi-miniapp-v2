{
  "devDependencies": {
    "@types/node": "^20.11.19",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "generate:docs": "ts-node --project scripts/tsconfig.json scripts/generate-structure.ts",
    "generate:types": "ts-node --project scripts/tsconfig.json scripts/generate-docs.ts"
  }
}